// First import { createContext } from "react"; so I can use and create createContext(); API.
import { createContext } from "react";

// I have asigned noteContext value to createContext(); function so I can use noteContext as a createContext.
const noteContext = createContext();

export default noteContext;